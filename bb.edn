{:paths ["src" "test"]
 :tasks
 {test {:doc "Run makura cljc actor boundary tests"
        :task (do
                (require 'clojure.test
                         'makura.murakumo-test)
                (let [run-tests (requiring-resolve 'clojure.test/run-tests)
                      r (run-tests 'makura.murakumo-test)]
                  (System/exit (if (zero? (+ (:fail r) (:error r))) 0 1))))}}}
