{:tasks
 {test {:doc "Run the complete standalone makura suite"
        :task (shell "clojure -M -m makura.test-runner")}
  audit {:doc "Validate canonical EDN and artifact boundaries"
         :task (shell "clojure -M scripts/audit.clj")}}}
